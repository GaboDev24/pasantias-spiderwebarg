/**
 * Indice del modulo API Client — Exporta todos los submodulos
 */

const sql = require('./sql');
const storage = require('./storage');
const ia = require('./ia');

module.exports = { sql, storage, ia };
